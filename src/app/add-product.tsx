import { ProductForm } from '@/components/ProductForm'; import { PlaceholderScreen } from '@/components/PlaceholderScreen'; import { useRole } from '@/hooks/useRole'; export default function AddProduct() { const { isAdmin } = useRole(); return isAdmin ? <ProductForm /> : <PlaceholderScreen title="Admin access required" description="Only an admin can add inventory products." icon="lock-closed-outline" />; }
