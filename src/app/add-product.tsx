import { ProductForm } from '@/components/ProductForm'; import { PlaceholderScreen } from '@/components/PlaceholderScreen'; import { useRole } from '@/hooks/useRole'; export default function AddProduct() { const { isOwner } = useRole(); return isOwner ? <ProductForm /> : <PlaceholderScreen title="Owner access required" description="Unlock the app as Owner to add inventory products." icon="lock-closed-outline" />; }
