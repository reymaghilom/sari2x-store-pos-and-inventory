import { createThemedStyles, useAppearance } from '@/store/appearance';
import { Card, PrimaryButton, ScreenContainer } from '@/components/ui'; import { spacing, typography } from '@/constants/theme'; import { Ionicons } from '@expo/vector-icons'; import { router } from 'expo-router'; import { Text, View } from 'react-native';
export function PlaceholderScreen({ title, description, icon = 'construct-outline', actionTitle, actionRoute }: { title: string; description: string; icon?: keyof typeof Ionicons.glyphMap; actionTitle?: string; actionRoute?: string }) {
  const { colors } = useAppearance();
  const styles = useStyles(); return <ScreenContainer><Card style={styles.card}><View style={styles.icon}><Ionicons name={icon} size={34} color={colors.primary} /></View><Text style={styles.title}>{title}</Text><Text style={styles.description}>{description}</Text>{actionTitle && actionRoute ? <PrimaryButton title={actionTitle} onPress={() => router.push(actionRoute as never)} style={{ alignSelf: 'stretch', marginTop: spacing.md }} /> : null}</Card></ScreenContainer>; }
const useStyles = createThemedStyles((colors) => ({ card: { alignItems: 'center', paddingVertical: spacing.xxxl, marginTop: spacing.xl }, icon: { width: 68, height: 68, borderRadius: 22, backgroundColor: colors.primarySoft, alignItems: 'center', justifyContent: 'center', marginBottom: spacing.lg }, title: { color: colors.text, fontSize: typography.title, fontWeight: typography.bold, textAlign: 'center' }, description: { color: colors.textMuted, fontSize: typography.body, lineHeight: 22, textAlign: 'center', marginTop: spacing.sm } }));
